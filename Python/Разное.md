## Построение диаграммы наследования

Требуется `pylint`
~~~~bash
pyreverse --colorized -mn  services/wildberries/schemas && dot -Tpng classes.dot > output.png
~~~~


## lru_cache

~~~~python
import bcrypt
from functools import lru_cache
from time import time

@lru_cache()
def slow_hash(data, rounds=16):
    salt = bcrypt.gensalt(rounds=rounds)
    hashed = bcrypt.hashpw(data.encode("utf-8"), salt)
    return hashed

# Пример использования
passwords = ["123", "456", "123", "456", "123", "456"]


def print_time_elapsed(start_time):
    elapsed_time = time() - start_time
    print(f"Time elapsed: {elapsed_time:.6f} seconds")

for password in passwords:
    start = time()
    print(slow_hash(password))
    print_time_elapsed(start)
    print("-------------------------")
~~~~